ans = .40*0.01 + .25*0.02 + .35*0.15
# q1---------------
messages = c(.40, .25, .35)
errors = c(.01, .02, 0.15)
prob = messages*erros
sum(prob)

# q2 --------------
code = c(.15,.20,.25,.40)
errors = c(.05,.03,.02,.01)
prob = code*errors
sum(prob)
